twig:
    default_path: '%kernel.project_dir%/templates'
    form_themes: ['bootstrap_4_layout.html.twig']
    file_name_pattern: '*.twig'
    paths:
        '%kernel.project_dir%/vendor/bretrzaun/statuspage/resources/views': StatusPage

when@test:
    twig:
        strict_variables: true
